{
  "name": "routing",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/game-scheduler/admin/routing/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:game-scheduler", "domain:admin", "type:routing"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/game-scheduler/admin/routing"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
